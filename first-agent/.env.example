# OpenAI (GPT Realtime)
OPENAI_API_KEY=your_openai_api_key

# Gemini Live - 後端驗證與 config API 用
# 需先執行: gcloud auth application-default login
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# 前端預填 Gemini 專案 ID（可選，避免 config API 未連線時為空）
VITE_GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Email MCP Server (可選，npm run dev-full 會自動啟動本地服務器)
# 本地服務器會在 http://localhost:8080/mcp 啟動
# 需要在 mcp_sent_mail/.env 中設定 EMAIL_ACCOUNT 和 EMAIL_PASSWORD
# EMAIL_MCP_URL=http://localhost:8080/mcp  # 預設使用本地服務器（npm run dev-full 時）
# EMAIL_MCP_DISABLED=true  # 設為 true 可禁用 email MCP 服務器
